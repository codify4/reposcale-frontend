export const metadata = {
    title: 'blog | reposcale',
    description: 'Blog platform for reposcale, a tool for sharing private repositories securely',
}

export default function SettingsLayout({ children }: { children: React.ReactNode }) {
    return (
        <>
            {children}
        </>
    )
}